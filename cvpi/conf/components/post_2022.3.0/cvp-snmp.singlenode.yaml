# Use this config file for CVP 2022.3.0 and later
cvp-snmp:
  bundle: {
            "snmpd-monitor-image",
            "snmpd-monitor"
          }

snmpd-monitor-image:
  actions:
    load:
      command: [ "%{CVP_HOME}/bin/image-load.sh", "net_snmp_image" ]
      nodes: { "*" }
      deps:
        containerd: start
      timeout: 300s

snmpd-monitor:
  kubernetes: true
  status:
    timeout: 120s
  actions:
    start:
      deps:
        snmpd-monitor-image: load
        kubernetes: start